<h1>Todos</h1>

<% @todos.each do |todo| %>
  <div class="well">
      <%= todo.description %>
      <%= todo.completed %>
      <%= link_to 'Ver', todo , class:'btn btn-primary'%>
  </div>
<% end %>
<%= link_to 'Agregar Nuevo', todos_new_path %>
